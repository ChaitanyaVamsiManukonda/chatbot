# Example environment variables for chatbot
# Replace the values with your API keys before deploying or running locally
# DO NOT COMMIT a .env file with real secrets to git!

# Site authentication password (required for login)
SITE_PASSWORD=your_password_here

# AI API keys (for model calls)
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Audio transcription (optional server-side transcription with OpenAI)
TRANSCRIBE_WITH_OPENAI=false

# Notes:
# - SITE_PASSWORD: Set this to protect your chat interface. For local dev, use a simple password like "12345".
#   For production, set a strong password in the Netlify dashboard environment variables.
# - TRANSCRIBE_WITH_OPENAI: If true, requires OPENAI_API_KEY and the `form-data` npm package (already installed).
# - Never commit a .env file with real secrets to source control. Use .gitignore to exclude .env.
