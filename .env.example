# Example environment variables for chatbot
# Replace the values with your API keys before deploying or running locally
OPENAI_API_KEY=your_openai_api_key_here
ANTHROPIC_API_KEY=your_anthropic_api_key_here
# Set to true to enable server-side transcription using OpenAI's audio transcription endpoint
TRANSCRIBE_WITH_OPENAI=false

# Notes:
# - If you enable TRANSCRIBE_WITH_OPENAI=true, install the `form-data` package so the Netlify function can send multipart requests:
#     npm install form-data
# - Keep your API keys secret. Don't commit real keys to source control.
